<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurants - Admin Panel</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
    
        body{
      font-family: "Rubik", sans-serif;
        }
        .table-responsive {
            overflow-x: auto;
        }
        .action-buttons .btn {
            margin-right: 5px;
        }
        .page-header {
            border-bottom: 1px solid #e3e6f0;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
        }
        .table th {
            background-color: #f8f9fc;
        }
        .table-hover tbody tr:hover {
            background-color: #f8f9fc;
        }
        .btn-group-sm > .btn, .btn-sm {
            padding: 0.25rem 0.5rem;
            font-size: 0.875rem;
            line-height: 1.5;
            border-radius: 0.2rem;
        }
    </style>
</head>
<body class="bg-light">
    <div class="container py-4">
        <div class="page-header d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="h3 mb-0 text-gray-800">Order Management</h1>
            </div>
            <div class="button-group">
                <%= link_to admin_dashboard_path, class: "btn btn-secondary" do %>
                    <i class="fas fa-arrow-left me-1"></i> Back to Dashboard
                <% end %>
            </div>
        </div>

        <div class="card shadow">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Restaurant List</h6>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-bordered table-hover mb-0">
                        <thead>
                            <tr>
                                <th class="text-center">ID</th>
                                <th class="text-center">Table</th>
                                <th class="text-center">User</th>
                                <th class="text-center">Amount</th>
                                <th class="text-center">Status</th>
                                <th class="text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% @orders.each do |order|%>
                                <tr>
                                    <td class="text-center"><%= order.id %></td>
                                    <td class="text-center">Table <%= order.restaurant_table.id %></td>
                                    <td class="text-center"><%= order.user.first_name %> <%= order.user.last_name %></td>
                                    <td class="text-center">₹<%= order.order_amount %></td>
                                    <td class="text-center"><%= order.order_status %></td>
                                    <td class="text-center action-buttons">
                                        <div class="btn-group btn-group-sm">
                                            <%= link_to admin_restaurant_order_path(@restaurant,order), class: "btn btn-secondary text-white" do %>
                                                <i class="fas fa-edit"></i>
                                            <% end %>
                                        </div>
                                    </td>
                                </tr>
                            <% end %>

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
