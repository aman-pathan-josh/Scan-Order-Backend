<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Restaurant Management</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    body{
      font-family: "Rubik", sans-serif;
    }
    .card {
      border-radius: 15px;
    }
    .form-control:focus, .form-select:focus {
      border-color: #4f46e5;
    }
    .btn-primary {
      background-color: #4f46e5;
      border-color: #4f46e5;
    }
    .btn-primary:hover {
      background-color: #4338ca;
      border-color: #4338ca;
    }
    .btn-success {
      background-color: #10b981;
      border-color: #10b981;
    }
    .btn-success:hover {
      background-color: #059669;
      border-color: #059669;
    }
  </style>
</head>
<body class="bg-light">
          <div class="card-header bg-white p-4 border-bottom-0">
            <h3 class="mb-0"><i class="fas fa-utensils me-2 text-primary"></i>Restaurant Details</h3>
            <p class="text-muted mb-0">Enter information about the restaurant</p>
          </div>
          <div class="card-body p-4">
            <%= form_with(model: [:admin, @restaurant], class: "needs-validation") do |f| %>
              <% if @restaurant.errors.any? %>
                <div class="alert alert-danger" role="alert">
                  <h5 class="alert-heading">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    <%= pluralize(@restaurant.errors.count, "error") %> prohibited this restaurant from being saved:
                  </h5>
                  <ul class="mb-0 ps-3">
                    <% @restaurant.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                    <% end %>
                  </ul>
                </div>
              <% end %>

              <div class="row g-4">
                <div class="col-md-6">
                  <div class="form-floating mb-3">
                    <%= f.text_field :restaurant_name, class: "form-control", placeholder: "Restaurant Name", id: "floatingName" %>
                    <%= f.label :restaurant_name, for: "floatingName" %>
                  </div>
                </div>
                
                <div class="col-md-6">
                  <div class="form-floating mb-3">
                    <%= f.text_field :contact, class: "form-control", placeholder: "Contact", id: "floatingContact" %>
                    <%= f.label :contact, for: "floatingContact" %>
                  </div>
                </div>
                
                <div class="col-12">
                  <div class="form-floating mb-3">
                    <%= f.text_area :address, rows: 3, class: "form-control", style: "height: 100px", placeholder: "Address", id: "floatingAddress" %>
                    <%= f.label :address, for: "floatingAddress" %>
                  </div>
                </div>
              </div>

              <div class="d-flex justify-content-end mt-4">
                <%= link_to admin_restaurants_path, class: "btn btn-outline-secondary me-2" do %>
                  <i class="fas fa-arrow-left me-1"></i> Cancel
                <% end %>
                <button type="submit" class="btn btn-success">
                  <i class="fas fa-save me-1"></i> Save Restaurant
                </button>
              </div>
            <% end %>
          </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
