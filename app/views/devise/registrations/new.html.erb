<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    :root {
      --primary-color: #4e73df;
      --primary-dark: #3a5fc8;
      --secondary-color: #f8f9fc;
      --accent-color: #ff6b6b;
    }
    
    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
      font-family: "Rubik", sans-serif;
    }
    
    .signup-container {
      background-color: white;
      border-radius: 10px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      width: 100%;
      max-width: 500px;
    }
    
    .signup-header {
      background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
      padding: 30px;
      text-align: center;
      color: white;
    }
    
    .signup-header h1 {
      font-size: 24px;
      font-weight: 700;
      margin-bottom: 0;
    }
    
    .signup-subtitle {
      color: rgba(255, 255, 255, 0.8);
      font-size: 14px;
      margin-top: 5px;
    }
    
    .signup-body {
      padding: 30px;
    }
    
    .form-floating {
      margin-bottom: 20px;
    }
    
    .form-control:focus {
      border-color: var(--primary-color);
      box-shadow: 0 0 0 0.25rem rgba(78, 115, 223, 0.25);
    }
    
    .password-container {
      position: relative;
    }
    
    .password-requirement {
      font-size: 12px;
      color: #6c757d;
      margin-top: 5px;
      font-style: italic;
    }
    
    .custom-checkbox {
      cursor: pointer;
    }
    
    .btn-signup {
      background-color: var(--primary-color);
      border-color: var(--primary-color);
      font-weight: 600;
      padding: 10px 0;
      margin-top: 20px;
      transition: all 0.3s;
    }
    
    .btn-signup:hover {
      background-color: var(--primary-dark);
      border-color: var(--primary-dark);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(78, 115, 223, 0.4);
    }
    
    .alternative-links {
      text-align: center;
      margin-top: 20px;
      padding-top: 20px;
      border-top: 1px solid #eee;
    }
    
    .food-icon {
      font-size: 40px;
      margin-bottom: 15px;
      color: var(--accent-color);
    }
    
    .alert-notification {
      font-size: 0.875rem;
      padding: 0.5rem;
      margin-top: -15px;
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      background-color: #f8d7da;
      border-color: #f5c2c7;
      color: #842029;
      border-radius: 0.25rem;
    }
    
    .error_icon {
      width: 16px;
      height: 16px;
      margin-right: 8px;
    }
    
    .name-row {
      display: flex;
      gap: 15px;
    }
    
    .name-col {
      flex: 1;
    }
    
    @media (max-width: 576px) {
      .name-row {
        flex-direction: column;
        gap: 0;
      }
    }
  </style>
</head>
<body>
  <div class="signup-container">
    <div class="signup-header">
      <div class="food-icon">
        <i class="fas fa-utensils"></i>
      </div>
      <h1>Create Account</h1>
      <div class="signup-subtitle">Join us and enjoy hassle-free ordering</div>
    </div>
    
    <div class="signup-body">
      <%= render "devise/shared/errors", resource: resource %>
      
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <div class="name-row">
          <div class="name-col">
            <div class="form-floating mb-3">
              <%= f.text_field :first_name, class: "form-control", id: "floatingFirstName", placeholder: "John", required: true %>
              <%= f.label :first_name, "First name", for: "floatingFirstName" %>
            </div>
          </div>
          
          <div class="name-col">
            <div class="form-floating mb-3">
              <%= f.text_field :last_name, class: "form-control", id: "floatingLastName", placeholder: "Doe", required: true %>
              <%= f.label :last_name, "Last name", for: "floatingLastName" %>
            </div>
          </div>
        </div>
        
        <div class="form-floating mb-3">
          <%= f.telephone_field :phone_number, class: "form-control", id: "floatingPhone", placeholder: "+1 (555) 123-4567", required: true %>
          <%= f.label :phone_number, "Phone Number", for: "floatingPhone" %>
        </div>
        <% if resource.errors.any? %>
          <% resource.errors.full_messages_for(:phone_number).each do |message| %>
            <p class="alert-notification"><i class="fas fa-exclamation-circle error_icon"></i> <%= message %></p>
          <% end %>
        <% end %>
        
        <div class="form-floating mb-3">
          <%= f.email_field :email, class: "form-control", id: "floatingEmail", placeholder: "name@example.com", required: true %>
          <%= f.label :email, "Email address", for: "floatingEmail" %>
        </div>
        <% if resource.errors.any? %>
          <% resource.errors.full_messages_for(:email).each do |message| %>
            <p class="alert-notification"><i class="fas fa-exclamation-circle error_icon"></i> <%= message %></p>
          <% end %>
        <% end %>
        
        <div class="form-floating mb-3">
          <%= f.password_field :password, class: "form-control", id: "floatingPassword", placeholder: "Password", required: true %>
          <%= f.label :password, "Password", for: "floatingPassword" %>
        </div>
        <% if resource.errors.any? %>
          <% resource.errors.full_messages_for(:password).each do |message| %>
            <p class="alert-notification"><i class="fas fa-exclamation-circle error_icon"></i> <%= message %></p>
          <% end %>
        <% end %>
        
        <div class="form-floating mb-3">
          <%= f.password_field :password_confirmation, class: "form-control", id: "floatingPasswordConfirmation", placeholder: "Confirm Password", required: true %>
          <%= f.label :password_confirmation, "Confirm Password", for: "floatingPasswordConfirmation" %>
        </div>
        <% if resource.errors.any? %>
          <% resource.errors.full_messages_for(:password_confirmation).each do |message| %>
            <p class="alert-notification"><i class="fas fa-exclamation-circle error_icon"></i> <%= message %></p>
          <% end %>
        <% end %>
        
        <%= f.submit "Sign up", class: "btn btn-primary btn-signup w-100" %>
        
        <div class="alternative-links">
          <%= render "devise/shared/links" %>
        </div>
      <% end %>
    </div>
  </div>
  
  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
