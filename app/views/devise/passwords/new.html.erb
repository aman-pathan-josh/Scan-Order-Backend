<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <style>
             body{
        font-family: "Rubik", sans-serif;
        }
        :root {
      --primary-color: #4e73df;
      --primary-dark: #3a5fc8;
      --secondary-color: #f8f9fc;
      --accent-color: #ff6b6b;
    }
    .login-header {
      background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
      padding: 30px;
      text-align: center;
      color: white;
      border-top-left-radius: 5px;
      border-top-right-radius: 5px;
    }
        .login-header h1 {
      font-size: 24px;
      font-weight: 700;
      margin-bottom: 0;
    }
        .login-subtitle {
      color: rgba(255, 255, 255, 0.8);
      font-size: 14px;
      margin-top: 5px;
    }
    .btn-reset {
      background-color: var(--primary-color);
      border-color: var(--primary-color);
      font-weight: 600;
      padding: 10px 0;
      margin-top: 20px;
      transition: all 0.3s;
      color: #fff;
      font-size: 16px;
    }
    
    .btn-reset:hover {
        color: #fff;
      background-color: var(--primary-dark);
      border-color: var(--primary-dark);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(78, 115, 223, 0.4);
    }
  </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-5">
                <div class="card shadow mt-5">
                    <div class="login-header">
                        <h1 class="text-center mb-4">Resend Password</h1>
                        <p class="login-subtitle">Enter your email and we'll send you reset password instructions</p>
                      </div>
                    <div class="card-body p-4">
                        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
                            <div class="mb-3">
                                <%= f.label :email, "Email Address", class: "form-label" %>
                                <%= f.email_field :email, class: "form-control", required: true, placeholder: "name@example.com", autofocus: true %>
                            </div>
                            
                            <%= render "devise/shared/error_messages", resource: resource %>
                            
                            <div class="d-grid gap-2 mb-3">
                                <%= f.submit "Reset Password", class: "btn btn-reset btn-lg" %>
                            </div>
                            
                            <div class="text-center">
                                <%= render "devise/shared/links" %>
                            </div>
                        <% end %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
